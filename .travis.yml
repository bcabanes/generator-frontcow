language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-frontcow' ]; then cd .. && eval "mv $currentfolder generator-frontcow" && cd generator-frontcow; fi

